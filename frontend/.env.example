# Base URL of the Salon backend. Do not include /api or a trailing slash.
VITE_API_URL=http://localhost:5000

# Must match backend CLIENT_URL. Vite will fail instead of silently switching ports.
VITE_DEV_PORT=5175
